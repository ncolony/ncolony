SCHEMA_METADATA = object()

def schemattr(schema, default=attr.NOTHING):
    

def load(fname, cls):
    with io.open(fname, encoding='utf-8') as fp:
        content = fp.read()
    parsed = json.loads(content)
    
    

def dumps(
    
